<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Force same light theme in night mode for consistent Material UI -->
    <style name="Base.Theme.TickTickAndroid" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/PrimaryColor</item>
        <item name="colorOnPrimary">@color/Text_icons</item>
        <item name="colorSecondary">@color/AccentColor</item>
        <item name="colorSurface">@color/white</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/DarkPrimaryColor</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>
</resources>